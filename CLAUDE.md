# Simple Activity Tracker

Cross-platform (Android-first, iOS later) running app in **Flutter**: live GPS metrics (speed, pace, splits), GPX track logging, eventually a customizable live display.

**Read [docs/PLAN.md](docs/PLAN.md) before doing any work** — it holds the phased plan, the verified machine environment, Windows-specific setup gotchas, and per-phase acceptance criteria. For the web app, API and phone-to-server sync work, **also read [docs/WEB-PLAN.md](docs/WEB-PLAN.md)** — reviewed and approved, with its own phases (W0–W4), decisions (§12) and working agreements (§13, including: prompt before installing anything on the machine).

For a non-technical walkthrough of the architecture and how the live metrics are calculated (polling rate, outlier filtering, split interpolation), see [docs/how-it-works.pdf](docs/how-it-works.pdf) — source at [docs/how-it-works.html](docs/how-it-works.html). Regenerate the PDF after editing the HTML with:
```
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless --disable-gpu --no-pdf-header-footer --print-to-pdf-no-header --print-to-pdf="docs/how-it-works.pdf" "file://$(pwd)/docs/how-it-works.html"
```

Repo: [github.com/sjefferson99/simple-activity-tracker](https://github.com/sjefferson99/simple-activity-tracker) (renamed from `simple-runner` on 2026-09-05 — GitHub redirects the old clone URL). GHCR image: `ghcr.io/sjefferson99/simple-activity-tracker-server` (the old `simple-runner-server` package is left in place, unused, not deleted).

## Current status

- **Phase 0 (MVP: live GPS speed on Android) — DONE (2026-09-02).** Verified on the Pixel8_API36 emulator (stationary fix correctly showed 0.0 km/h) and on a physical Samsung S10 over USB (real walking speed tracked correctly). `flutter analyze` and `flutter test` clean (13 tests), Snyk code scan clean.
- **Phase 1 (metrics, splits, controls, GPX logging) — DONE (2026-09-02).** MetricsEngine (elapsed/distance/speed/splits with interpolated crossing times), TrackingController state machine (idle/tracking/paused/finished), MetricSpec-driven tile grid, km/h ⇄ min/km toggle, RunGpxLog (crash-safe atomic-rename flush, pause → new `<trkseg>`). Verified on a physical Samsung S10: pause/resume correctly produced 2 track segments in the written GPX file, 34 real GPS points logged. `flutter analyze` and `flutter test` clean, Snyk clean. (Test count has since grown to 29 with the review's regression tests.)
- **Phase 1 code review — DONE (2026-09-02), all findings fixed.** Worth knowing what was wrong, since some of it is easy to reintroduce: concurrent `RunGpxLog.flush()` calls shared one hardcoded `.tmp` path and could truncate the end of a run (flushes are now chained); run filenames were minute-resolution so two runs in a minute overwrote each other (now seconds + collision suffix); a throwing final flush stranded the wakelock with the screen forced on (now released in a `finally`); split average speed could be `Infinity` on a zero-duration split; the current-speed window pruned to a single point whenever fixes arrived >3s apart, blanking the speed readout under weak GPS (now always keeps the last two); and `LiveRunAcquiring` had no Stop control despite already holding the wakelock (now has Cancel). 29 tests, analyze and Snyk clean.
- **Debug banner hidden (2026-09-02)** via `debugShowCheckedModeBanner: false`. Note this only hides the ribbon — device builds are still debug-compiled Dart, so performance is not representative of release.
- **iOS groundwork — VERIFIED on iOS Simulator (2026-09-02).** `ios/Runner/Info.plist` has `NSLocationWhenInUseUsageDescription`, `NSLocationAlwaysAndWhenInUseUsageDescription`, and `UIBackgroundModes: location`; `GeolocatorLocationService` branches to `AppleSettings` (fitness activity type, background updates) instead of forcing `AndroidSettings` everywhere. First Mac build (`flutter build ios --debug --simulator`) succeeded cleanly, including `pod install` — no fixes needed. Ran on an "iPhone 17" simulator (iOS 26.5): app launched, correctly showed the native location-permission dialog with the Info.plist description text, and after granting reached the same idle "Press Start to begin tracking" screen as Android. **Still not tested on a physical iPhone** — that's this week, per plan.
- **Mac dev environment set up (2026-09-02).** Flutter 3.47.2 and CocoaPods installed via Homebrew (`brew install --cask flutter`, `brew install cocoapods`) — both land on PATH automatically via the existing `brew shellenv` eval in `~/.zprofile`, no manual PATH edits needed. Xcode installed from the App Store (Command Line Tools alone is not enough — `flutter doctor` reports "Xcode installation is incomplete" until the full app is installed and `sudo xcode-select --switch /Applications/Xcode.app/Contents/Developer` + `sudo xcodebuild -runFirstLaunch` are run). iOS simulator runtimes are a separate ~8.5GB download on top of Xcode itself (`xcodebuild -downloadPlatform iOS`) — Xcode being "installed" doesn't mean a runtime is ready to boot. Android Studio also installed via Homebrew (`brew install --cask android-studio`) to get a second, independent Android toolchain on the same Mac; its SDK Manager did **not** install `cmdline-tools` by default even though it can build and run apps — `flutter doctor` needs it explicitly ticked on the SDK Tools tab (cmdline-tools missing shows as "Unable to locate Android SDK" style errors even with an otherwise-working SDK). Both toolchains coexist fine on one machine: this Mac can now build/run iOS (simulator only so far) and Android (verified against a physical Samsung SM-G975F over USB) independently of the Windows desktop.
- **GPS outlier filtering + duplicate-fix bug — FIXED (2026-09-02).** Found by running the iOS Simulator's built-in "City Run" location scenario, which starts from a stale fix and so injects a ~65km teleport between the first two fixes. Three distinct bugs, all worth knowing since the reasoning is easy to get wrong:
  - **No outlier rejection at all.** `MetricsEngine` only filtered on accuracy and non-positive duration, so one teleport permanently poisoned cumulative distance/average speed (showed 60.57km and 4101 km/h after 53s). Current speed self-heals because it is windowed; distance and `_movingElapsed` have no windowing or decay, so they never recover. It also manufactured ~65 phantom splits, the last of which got a sub-millisecond interpolated duration — that was the "Last split 0:00" symptom, not a formatting bug.
  - **Rejecting a point must not pin the anchor.** The first fix kept `_lastAccepted` on the pre-jump point, so every subsequent *good* fix near the teleport's landing spot also looked impossible and was rejected too — a real run lost ~77s of clean data before the implied speed against the stale anchor decayed under the threshold. Two consecutive rejects that agree with each other now re-anchor. Re-anchoring credits **no** distance or elapsed time (the path between is unknowable, so it is a discontinuity like pause/resume), clears `_recentPoints` (or the speed tile reads the teleport's implied speed for seconds afterwards), and requires ≥2m of motion between the pair — a GPS stuck repeating one wrong position implies 0 m/s, which "agrees" trivially and would otherwise hand the anchor to the bad location.
  - **Speed alone is not a sufficient test.** A long enough gap makes any teleport look slow (65km in an hour is ~18 m/s), so there is also an absolute 20km per-segment cap and a 30s TTL on the re-anchor candidate. The cap has to stay generous: a legitimately sparse stretch of fixes (tunnel, backgrounded app) can cover kilometres between fixes, and an earlier 1km cap broke the multi-split test.
  - **Every GPS fix was logged twice.** `LiveRunController.start()` never tore down the previous run before allocating a new one, and `LocationService.stream` opens a *fresh* platform stream per access rather than sharing one — so a re-entrant `start()` (double-tapped Start, or `startNewRun()` without a `stop()`) stacked a second listener and double-counted every fix into both the metrics engine and the GPX file (442 trackpoints for 221 fixes). It also leaked the flush timer and dropped the previous `RunGpxLog` unfinalized. `start()` now calls `_disposeRun()` first.
- **GPX export to public storage — DONE (2026-09-02).** MVP's last piece: exported runs weren't reachable outside the app. Android now copies each finished run's GPX into public `Downloads/SimpleRunner/` via the `media_store_plus` plugin (`RunExportService`) — no storage permission needed on API 30+, since it writes through `MediaStore` rather than a direct path. iOS instead exposes the app's existing private Documents folder to the Files app (`UIFileSharingEnabled` + `LSSupportsOpeningDocumentsInPlace` in Info.plist) — no code change needed there, since `RunGpxLog` already wrote to Documents. A "Where's my file?" link on the run-summary screen (`features/export_help/`) opens plain, platform-specific steps. Verified end-to-end on the physical Samsung: file appeared at the documented path and was independently found via the stock Files app.
  - **`media_store_plus` needed a Gradle patch to build at all.** Its `android/build.gradle` hardcodes `compileSdkVersion 33`; this project's resolved AndroidX dependency versions (pulled in transitively, not from anything we added) now require compileSdk 34+, so the plugin's own `:checkDebugAarMetadata` task fails before our app even builds. Fixed with an `afterEvaluate` block in `android/build.gradle.kts` that force-bumps just that subproject's `compileSdk` to 36. **This had to be `afterEvaluate`, not `plugins.withId(...)`** — the latter runs before the plugin's own Groovy `build.gradle` sets `compileSdkVersion 33`, so it gets silently clobbered; `afterEvaluate` runs strictly after, so it wins.
  - **Known risk, not yet a problem:** `media_store_plus` was last published ~2 years ago (v0.1.3) and the build also prints "Future versions of Flutter will fail to build if your app uses plugins that apply KGP [Kotlin Gradle Plugin]" naming this plugin specifically. Works today; a future Flutter upgrade could break it outright with no upstream fix available. If that happens, the options are: patch further, fork the plugin, or replace it with a hand-written MediaStore platform channel (more work, no dependency risk).
- **Phase 2 and beyond — NOT STARTED** (see docs/PLAN.md §6).
- **Web app + sync (docs/WEB-PLAN.md) — W0 DONE (2026-09-02), merged to `main` via #1.** Flutter app moved to `mobile/`; FastAPI server skeleton in `server/` with `/healthz`, Alembic wired to an empty baseline migration, non-root multi-stage Dockerfile; `deploy/docker-compose.yml` for local dev; three GitHub Actions workflows (Flutter, Server, Container) all green on the PR and on `main`. First image pushed to `ghcr.io/sjefferson99/simple-runner-server:latest` (private by default — pull needs `docker login ghcr.io`, or the package can be made public later from GitHub package settings if that's wanted).
- **Web app W1 (data, auth, runs API, analysis) — DONE (2026-09-02), merged to `main` via #2.** Users/device_tokens/runs/run_analyses tables via a second Alembic migration; repository layer (protocols + SQLAlchemy impls) for all four; `LocalFileBlobStore` under `SR_DATA_DIR/gpx/<user_id>/<uuid>.gpx`. Auth: argon2id passwords (`pwdlib`), opaque device tokens (SHA-256 stored, `srdt_` prefix), signed session cookies (`itsdangerous`), a `CurrentUser`/`CurrentAdmin` dependency pair, an in-memory login rate limiter, and env-based admin bootstrap. Full `/api/v1` surface from the plan's §5.2 table: auth/me/devices/password, runs CRUD + gpx/analysis/track, admin user management with self/last-admin guards. `AnalyzerV1` (elevation smoothing, splits with interpolated crossings, best-effort windows, a moving-time-based chart series) verified against a synthetic 3km fixture with known answers. 52 tests, `mypy --strict` and ruff clean, `openapi.json` committed with a CI drift check, Snyk clean (18 low-severity "hardcoded password" hits, all in test fixture literals, none in `app/`). Verified end-to-end with `docker compose up` + curl: login, upload with real analysis, idempotent re-upload, byte-identical GPX download.
  - **The `login_rate_limiter` and `get_settings()`/`get_engine()` are all process-level singletons** (the settings/engine ones `lru_cache`'d, the rate limiter a plain module instance) — every one of them needed an explicit reset in the test fixture (`tests/conftest.py`), or state leaked between tests in ways that looked like unrelated failures (a `429` on a test that had nothing to do with rate limiting; a stale DB engine pointed at a deleted tmp file). Any new module-level singleton in `app/` needs the same treatment before it'll play well with per-test isolation.
  - **`session.delete()` on two rows with a plain `ForeignKey` (no `relationship()`) is not safely ordered.** Deleting a `RunAnalysis` then its parent `Run` in the same flush intermittently violated the FK constraint, because SQLAlchemy's unit-of-work only orders deletes using a configured `relationship()` dependency graph — with none declared, it flushed in whatever order it liked. Fixed by calling `session.flush()` right after each child delete, before deleting the parent. Same fix needed in two places (`runs.py`'s `delete_run`, `admin.py`'s `delete_user`, which also needed to hard-delete the target's device tokens before deleting the user row).
  - **FastAPI's default `HTTPException` handler nests `detail` under a `"detail"` key**, so `api_error()`'s intended `{"error": {"code", "message"}}` shape was actually arriving as `{"detail": {"error": {...}}}` — silently wrong until a test actually asserted on `response.json()["error"]`. Fixed with a custom `@app.exception_handler(HTTPException)` in `main.py` that unwraps it, and also normalizes any plain-string-`detail` exception (framework/validation errors we don't construct ourselves) into the same shape.
  - **The synthetic GPX fixture's original ±3m/point jitter inflated naive point-to-point distance by ~30%** at the fixture's 1-second sample rate, because `AnalyzerV1` deliberately does no path smoothing (only elevation gets that treatment, per the plan). Jitter tightened to ±0.5m — real GPS noise is a small perturbation around the true path, not comparable in size to the distance actually covered between fixes at this sample rate. `tests/fixtures/generate_sample_run.py` regenerates `sample_run.gpx` if this ever needs revisiting.
  - Series `t_s` in `AnalyzerV1`'s output tracks accumulated *moving* time, not wall-clock elapsed — it doesn't advance across the fixture's inserted 90s segment gap, matching how `moving_seconds` and the splits already treat that gap. Documented in `v1.py`'s `_series()` docstring since a future reader would reasonably expect wall-clock instead.
  - **`git mv android mobile/android` failed with "Permission denied"** even with no Gradle daemon running (`./gradlew --stop` reported none). Root cause: `android/.gradle/` (gitignored build cache) held a lock on the directory handle that blocked renaming the parent, even though every individual tracked file/subfolder inside `android/` moved fine on retry. Fix: move each top-level item individually with `git mv` (tracked files) / `mv` (the few gitignored ones: `gradlew`, `gradlew.bat`, `local.properties`, `*.iml`) instead of the whole directory in one call; leave the untracked `.gradle`/`.kotlin` cache dirs behind under the old `android/` path (they regenerate under `mobile/android/` on the next build) rather than fighting the lock.
  - **`.gitignore` had to split.** Flutter's own ignores (`/build/`, `/coverage/`, etc.) are root-anchored and only work relative to the directory containing the `.gitignore` file, so they moved verbatim into a new `mobile/.gitignore`. The repo-root `.gitignore` now holds only generic/IDE ignores plus new entries for `server/.venv`, Python caches, `/data/` volumes, and `deploy/.env` (holds `SR_SECRET_KEY` — must never be committed).
  - Flutter isn't on the Bash tool's PATH in this environment (added to the interactive PowerShell user PATH only) — invoke it via PowerShell, or by full path `C:\git\flutter\bin\flutter.bat` if using Bash. Same story for `uv` after installing it via winget in this same session: use the full path under `%LOCALAPPDATA%\Microsoft\WinGet\Packages\astral-sh.uv_...\uv.exe` from Bash until a fresh shell picks up the PATH change.
  - **`app.config.Settings` and `app.db`'s engine must be lazy, not module-level singletons.** A `settings = Settings()` at import time (the obvious first draft) permanently bakes in whatever `SR_*` env vars were set at first import — fine for the real app (env vars are static for its process lifetime) but wrong for tests, where `pytest`'s `monkeypatch.setenv` per-test needs to actually take effect. Fixed with `get_settings()`/`get_engine()` behind `functools.lru_cache`, and the test fixture calls `.cache_clear()` on both before and after each test. Worth remembering before adding more settings-dependent module state in W1's auth/repositories.
  - **Multi-platform `docker buildx build` fails outright on the default `docker` driver** ("Multi-platform build is not supported for the docker driver") — needs `docker buildx create --driver docker-container --use` first (matches what `docker/setup-buildx-action` does in CI, so this was purely a local-verification gotcha, not a CI risk).
  - All dependencies (fastapi, uvicorn, sqlalchemy, alembic, pydantic-settings) have aarch64 manylinux wheels as the plan expected — the local `docker buildx build --platform linux/amd64,linux/arm64` finished in seconds with no QEMU-emulated compilation.
  - The `app/openapi_export.py` script and committed `openapi.json` are W1 deliverables (need real routers first) — `server.yml`'s drift-check step is present but commented out until then.
  - **The container.yml smoke-test step initially failed in CI**: `docker run` with no `-v` for `/data` crashes on startup (`sqlite3.OperationalError: unable to open database file`), because the Dockerfile's `VOLUME /data` only declares the mount point — with nothing bound to it, Docker creates an anonymous volume owned by root that the non-root `app` (uid 1000) user can't write into. `deploy/docker-compose.yml` already mounted `./data:/data` so this never showed up locally; fixed by adding the same `-v` to the CI smoke-test `docker run`. Any other place a bare `docker run` of this image shows up (docs, ad-hoc testing) needs the same mount.
- **Web app W2 (Web UI) — DONE (2026-09-02), merged to `main` via #3.** Server-rendered Jinja2 + htmx pages in `server/app/web/` and `server/app/templates/`: `/login`/`/logout`, `/` (run list with cursor-based "Load more"), `/runs/{id}` (headline phone stats, server analysis card, Leaflet map fed by `/api/v1/runs/{id}/track`, splits/best-effort tables, uPlot pace+elevation chart, title/notes edit, GPX download, delete-with-confirm), `/devices` (list/revoke), `/settings` (change password), `/register` (gated on `SR_ALLOW_REGISTRATION`), and `/admin/users` (create, promote/demote, disable/enable, reset password via `hx-prompt`, delete — all with the self/last-admin guards mirrored from the admin API). htmx, Leaflet 1.9.4 and uPlot 1.6.31 vendored under `server/app/static/vendor/` with each project's LICENSE (versions tracked in `VERSIONS.md`) — no Node build step. CSRF: every mutating web route requires `X-Requested-With: htmx`, set globally via `hx-headers` on `<body>`; `htmx.config.responseHandling` is overridden in `base.html` so 4xx/error fragments (wrong password, guard rejections) actually swap into the DOM, since htmx's default only swaps 2xx/3xx. Web pages authenticate via session cookie only (`app/web/deps.py`'s `get_web_user`/`get_web_admin`, distinct from the API's bearer-or-cookie `CurrentUser`) and redirect to `/login` (303) rather than 401 on failure. 25 new route tests (page render/redirect, CSRF 403, admin 404 for non-admins, self-demote and last-admin guards, disabling a user killing both their session cookie and device token), 77 total, `mypy --strict` and ruff clean, `openapi.json` unchanged (web routers use `include_in_schema=False` — the committed spec stays the mobile app's `/api/v1`-only contract), Snyk clean (29 low-severity "hardcoded password" hits, all test fixture literals, none in `app/`). Verified end-to-end via `docker compose up`: real login, GPX upload, map/splits/chart rendering, device revoke, and the admin/self-demote/last-admin guards all exercised with curl/Python against the built container.
  - **TestClient's cookie jar silently drops `Secure` cookies** because `TestClient` talks plain `http://testserver` and `Secure` cookies are only valid over TLS (enforced by the standard `http.cookiejar` the client wraps) — every session-cookie web test failed until `tests/conftest.py`'s `app_client` fixture set `SR_SECURE_COOKIES=false`, mirroring the real "plain-http LAN testing" setting from §5.5. Silent and confusing: requests still returned 200 with a `Set-Cookie` header, but the next request had no cookie at all, redirecting back to `/login` as if the login had failed.
  - **A plain `<form method="post">` never carries `hx-headers`** — that attribute only applies to requests htmx itself issues (`hx-post`/`hx-patch`/`hx-delete`/etc.), so the CSRF rule (§5.6) only works if every mutating control uses htmx attributes, not native HTML form submission. All mutating forms/buttons in the templates use `hx-post`/`hx-patch`/`hx-delete`, never bare `method="post"`.
  - Changing your own password re-mints the session cookie after bumping `sessions_invalidated_at` — otherwise the same request that changes the password would also invalidate the browser's own current session (since its `issued_at` predates the bump), logging the user out immediately after a successful change. Other sessions and every device token are still revoked as intended.
  - Deleting a run or a user from the web UI reuses the same guard/cascade logic as the corresponding API route (analysis-then-run delete ordering, blob cleanup, self/last-admin checks) but as independent route handlers in `app/web/`, not by calling the JSON API internally — the web layer talks to the repositories directly, same as the API layer does.
- **Web app W3 (mobile sync) — DONE (2026-09-02), merged to `main` via #4.** New packages (`http`, `flutter_secure_storage`, `connectivity_plus`, `uuid`, `package_info_plus`). `core/api/`: `ApiClient` interface + `HttpApiClient` (the only file that knows URLs/wire format) + DTOs (`RunDto`, `AnalysisDto`, `LoginResponseDto`, `DeviceDto`, `UserDto`) + a typed `ApiException` hierarchy (network/timeout/unauthorized/rate-limited/server/rejected) that `HttpApiClient._send()` maps every response onto. `core/auth/AuthService` is plain Dart (secure-storage-backed server URL + device token), wrapped for widgets by `AuthStateController` (`AsyncNotifier`). `core/sync/`: `RunStore`/`FileRunStore` (JSON sidecar next to each run's GPX, same stem, atomic temp+rename like `RunGpxLog`), `ConnectivityMonitor`/`PlusConnectivityMonitor`, and `SyncService` — a single-flight queue processor (oldest-first by `startedAt`) triggered by run-finished/app-resumed/connectivity-regained/retry-now, with in-memory 30s→1m→2m→5m→10m backoff per record and the exact retryable-vs-permanent split from the plan (network/timeout/5xx/429 retry; 401 signs the device out via `markSignedOutDueToAuthFailure` and leaves the record pending; other 4xx become `failed(retryable:false)`, excluded from automatic retry but included when the user taps "Retry now"). `LiveRunController.start()`/`stop()` now generate a `clientRunId`/UTC `startedAt`, write the `RunRecord` sidecar *before* the state switches to `LiveRunFinished`, then fire-and-forget `SyncService.runFinished()`. New `features/settings/SettingsScreen` (server URL, sign in/out, sync queue summary, Retry now) and a `RunSyncSection` on the run-finished screen (sync status line + an Insights section reading the server analysis' elevation/best-effort/distance/moving-time fields once uploaded). `WidgetsBindingObserver` in `app.dart` wires app-resume to `SyncService.onAppResumed()`. Android manifest gained `INTERNET`/`ACCESS_NETWORK_STATE` + `usesCleartextTraffic="true"`; iOS `Info.plist` gained `NSAppTransportSecurity.NSAllowsLocalNetworking`. 34 new tests (models, DTOs, `HttpApiClient` via `package:http/testing.dart`'s `MockClient`, `AuthService` via `flutter_secure_storage`'s `TestFlutterSecureStoragePlatform`, `SyncService` via fakes + an injectable clock, `FileRunStore` against a real temp dir), 86 total, `flutter analyze`/Snyk clean. **Verified end-to-end on the physical Samsung S10** against a real `docker compose up` server: sign-in, a finished run uploading and showing "Uploaded" on the summary screen, server log confirming `POST /api/v1/runs → 201` followed by the analysis fetch, and the same run rendering in the web UI (map/track data) in a browser on the same LAN.
  - **`flutter_secure_storage` 11.0.0 sets `compileSdk = 37`, but the installed Android SDK only offers `android-37.0`/`37.1`/`37.2`** — Google has apparently split API level 37 into sub-versions with no bare `android-37` target at all, so AGP can't resolve the plugin's plain-integer request and `:flutter_secure_storage:compileDebugJavaWithJavac` fails before the app builds. Same class of problem as the existing `media_store_plus` compileSdk patch (§ Phase 1 notes) — extended `android/build.gradle.kts`'s `afterEvaluate` fix into a reusable `pinCompileSdkToApp()` helper and applied it to both plugins, pinning each to `:app`'s own compileSdk rather than chasing the new sub-versioned platform names.
  - **A UX bug only showed up in real on-device testing, not unit tests**: `AuthStateController`'s mutation methods originally routed `signIn()`/`setServerUrl()` failures through Riverpod's `AsyncValue.guard()`, so a failed login turned the whole provider into `AsyncError` — and since `SettingsScreen` rendered the entire sign-in form from that same `AsyncValue`, a wrong password or network error made the form (and whatever the user had already typed) disappear completely, replaced by a bare error banner with no way to retry short of restarting the app. Fixed by having the mutation methods rethrow instead of touching `state`, and catching the exception locally in `_SignInFormState` to show an inline error next to the form, which stays mounted either way. Worth remembering for any future `AsyncNotifier` that backs an interactive form: never let a user-input error become the provider's `AsyncError`, since Riverpod's error case usually replaces the whole widget subtree built from `.when(data: ...)`.
  - **The real blocker during device testing wasn't code — it was the phone's Wi-Fi silently reconnecting to a guest network** (`Jefferson_guest`, isolated from the desktop's wired LAN) between sign-in attempts, producing a `SocketException: Software caused connection abort`. This burned significant time because the symptom (connection abort) looked identical to a firewall block, and Windows Firewall genuinely *was* blocking it too on the first attempt (fixed with an inbound-allow rule for TCP 8000) — two independent causes stacked, and disabling the firewall entirely still didn't fix it because the guest-network issue was the one still active. `netstat -ano | findstr :8000` showing an `ESTABLISHED` connection from the phone's real LAN IP (not the guest one) was the moment this became diagnosable. Lesson: when a phone-to-desktop LAN connection fails intermittently, check which Wi-Fi network the phone is actually on before assuming it's a firewall or app bug — a guest/isolated SSID reconnect is easy to miss since the phone still shows "connected to Wi-Fi."
  - Docker Desktop's port-forwarding proxy on Windows always shows connections in `docker logs` as originating from `127.0.0.1` (same-host) or the WSL2 bridge IP `172.19.0.1` (external clients relayed through the proxy) — never the real external IP. This means **you cannot distinguish "my own curl" from "the phone's request" by source IP in the container logs**; use `netstat -ano | findstr :<port>` on the Windows host instead, which does show genuine remote IPs for established connections.
- **GHCR image verified standalone (2026-09-03)**, ahead of W4. The `simple-runner-server` package was private (per the W0 note); flipped to **public** via the GitHub web UI (Settings → Package settings → Danger Zone → Change visibility) since neither `gh api`/`gh auth token` nor a plain `docker login ghcr.io` had the `read:packages` scope needed to pull or to flip visibility from the CLI — `gh auth refresh -s read:packages` would add it if CLI/API access to packages is ever needed again. After that, `docker pull ghcr.io/sjefferson99/simple-runner-server:latest` and a plain `docker run` (env vars + **bind-mounted** `-v <hostpath>:/data`, no compose file) round-tripped cleanly: migrations ran, `/healthz` returned `{"status":"ok",...}`, and a real `/api/v1/auth/login` returned a valid token. **Bind mounts work fine on Windows** — the earlier "Permission denied" writing to `/data` traced back to Git Bash's own path-mangling, not Docker/SQLite/Windows filesystem behavior:
  - **Git Bash auto-converts POSIX-looking CLI arguments to Windows paths**, including the *container-side* half of a `-v hostpath:/data` bind-mount spec — `-v /c/Users/steph/foo:/data` had its `/data` silently rewritten before Docker ever saw it, corrupting the mount so the app's uid-1000 user landed on an unmounted/root-owned `/data` and got `Permission denied`. Symptom looked exactly like the known "anonymous volume owned by root" gotcha (container.yml's smoke-test comment, §W0) but wasn't that at all. **Fix: prefix the `docker run`/`docker` command with `MSYS_NO_PATHCONV=1`** whenever a container-side path appears in the arguments (bind mounts, `docker exec ... ls /data`, etc.) — confirmed this alone took a mount from `Permission denied` to working with no other change.
  - A related red herring while diagnosing: `chmod 777` on the host directory from Git Bash **does not** reliably translate to real writability inside the container the way it does on CI's native Linux runner (container.yml's smoke test does exactly `mkdir -p /tmp/sr-smoke-data && chmod 777 ...` and that's real POSIX chmod there) — on Windows/NTFS via Docker Desktop's file sharing, permissions work differently, so a bind mount that still fails after `chmod 777` on Windows doesn't rule out a mount problem the way it would on Linux. Once `MSYS_NO_PATHCONV=1` was in place, no chmod was needed at all — a bare `mkdir -p` on the host dir was sufficient.
  - A host path must fall under one of Docker Desktop's shared drives/folders (Settings → Resources → File sharing) to bind-mount at all; `%USERPROFILE%`-relative paths (used here) and any path under the repo are safe bets and are what W4's compose files should use (e.g. `./data:/data` next to the compose file, gitignored, same pattern as the existing `deploy/data/` entry) — avoid inventing a path outside the repo like `C:\tmp\...` for anything meant to be reproducible from docs.
  - This clears the way for W4: the published image itself needs no fixes, and any deployment compose file can use a plain bind-mounted `./data:/data` (or a Pi's real Linux host mount) with confidence — just remember `MSYS_NO_PATHCONV=1` (or an equivalent) when *testing* those compose files' `docker run`/`docker compose` invocations from Git Bash on this Windows machine specifically; it's a Git-Bash-on-Windows-only concern, irrelevant on a Pi and in CI (native Linux shells, no path mangling).
- **Web app W4 (deployment guide) — DONE (2026-09-03), not yet committed.** `deploy/standalone-tls/`: an `app` service (unpublished, only reachable from `proxy` over the compose network on port 8000, running the **published** `ghcr.io/sjefferson99/simple-runner-server:latest` image — not built locally, unlike the plain dev `deploy/docker-compose.yml`) plus a `proxy` service (`nginx:alpine`) that terminates HTTPS with a self-signed cert (`generate-cert.sh`) and publishes `:80` (redirects to `:443`) and `:443`. This is the project's one deployment guide — no separate Traefik or Raspberry Pi doc, since the same multi-arch image and compose file work unchanged on a Pi, and the README documents how to swap in a different TLS-terminating reverse proxy later (delete `proxy`, point the new proxy at `app:8000`, adjust `SR_TRUSTED_PROXIES`) without touching the app service itself. Verified end-to-end (Windows/Docker Desktop, amd64), against both a locally-built image and the pulled GHCR image: HTTP→HTTPS redirect, `/healthz` 200 over TLS, and a real login returning a `Secure; HttpOnly; SameSite=lax` cookie — confirms `X-Forwarded-Proto` and `SR_TRUSTED_PROXIES` are wired correctly through nginx to the app. Not yet verified on a physical Pi or against a real phone upload over `https://`.
  - Generating the self-signed cert from Git Bash hit the same class of path-mangling bug as the `/data` bind-mount issue above: `openssl -subj "/CN=<host>"` was silently rewritten into a Windows path (`C:/Program Files/Git/CN=...`). Fixed in `generate-cert.sh` with a `//CN=` (double-leading-slash) escape on `msys`/`cygwin` — OpenSSL treats that identically to a single slash. Unlike the bind-mount case, `MSYS_NO_PATHCONV=1` did **not** fix this one (tried first, still failed) — the `-subj` value needed its own escape.
- **Mobile app fixed to actually work against `deploy/standalone-tls/` (2026-09-03).** Testing login against the new deployment surfaced two real bugs in `HttpApiClient`, both now fixed with tests (Snyk clean):
  - **`http://` silently returned the redirect's HTML body instead of an error.** `dart:io`'s `HttpClient` (which the plain `package:http` `Client` wraps) never auto-follows a redirect on a non-GET request — nginx's `http://` → `https://` redirect (a 301) on the login POST landed in `HttpApiClient._send` as a raw HTML body that the caller then tried to `jsonDecode`. Fixed: any 3xx response now throws `ApiRejectedException` with an explicit "try https://" message instead of falling through to a generic decode failure.
  - **The self-signed cert was rejected outright** — the default `http.Client()` uses the platform trust store, which has no reason to trust a cert `generate-cert.sh` just minted. Rather than a blanket "disable cert validation" toggle (a real MITM downgrade), added **trust-on-first-use certificate pinning**: `core/api/cert_trust_store.dart` (`CertTrustStore`, secure-storage-backed host→SHA-256-fingerprint pins) plus a custom `dart:io HttpClient` with a `badCertificateCallback` in `HttpApiClient` that only accepts a cert matching that exact host's pin — a cert from a different host, or a *different* cert later presented by the same host (rotation or a real MITM), is never silently trusted. On rejection the app throws `ApiCertificateException` (fingerprint + host); the Settings sign-in form catches it and shows a dialog with the fingerprint for the user to confirm against `generate-cert.sh`'s output before pinning and retrying.
  - The accept/reject decision itself is factored into a pure top-level function (`decideCertificateTrust` in `http_api_client.dart`) specifically because `X509Certificate` can't be constructed in a plain unit test (its factory is a VM intrinsic) — the pure function is the actually-tested seam, and `_acceptCertificate` is just glue that computes the SHA-256 DER fingerprint and calls it.
  - **Gotcha for any future `HttpApiClient` test:** `_send` now calls `CertTrustStore.ensureLoaded()`, which touches `flutter_secure_storage`'s platform channel — fine in real use, but it broke every existing `http_api_client_test.dart` test (`Binding has not yet been initialized`) because those tests construct `HttpApiClient(client: MockClient(...))` outside any Flutter test binding. Fixed by only calling `ensureLoaded()` when the client actually does its own cert-checking (i.e. no `client:` override was passed) — a test-injected transport can never trigger `badCertificateCallback` anyway, so skipping the store entirely there is correct, not just a workaround.
  - 95 tests total (mobile), `flutter analyze` clean.
- **MVP code & security review — DONE (2026-09-03), plans written.** Two handoff plans in `docs/`: [docs/SERVER-PRODUCTION-PLAN.md](docs/SERVER-PRODUCTION-PLAN.md) (take the server to production behind a reverse proxy) and [docs/MOBILE-QUALITY-PLAN.md](docs/MOBILE-QUALITY-PLAN.md) (internal-testing quality bar, not store release — not started). Read the relevant plan before starting any item; each has a `Do`/`Verify` and a delivery order.
- **Server production plan, batch 1 (S1→S3→S2) — MERGED (2026-09-03)**, PRs #8/#9/#10. `SR_SECRET_KEY` now required (≥32 chars, denylist of placeholders), `create_app()`/`cli.run()` fail fast on a bad one; malformed pagination cursors return 400 instead of a 500; server-side validation (`app/validation.py`) now mirrors the API's Pydantic constraints on every web form route (register, admin create/reset-password, settings password change, activity title/notes). Each verified in the real `deploy/standalone-tls` container before merge (see the workflow below).
- **Server production plan, batch 2 (S5 + D1) — MERGED (2026-09-04)**, PR #11. `SecurityHeadersMiddleware` (`app/security_headers.py`) adds CSP-with-nonce/HSTS-when-secure/nosniff/referrer-policy/permissions-policy/COOP/no-store to every response; `deploy/standalone-tls/nginx.conf` hardened (`server_tokens off`, `http2 on`, ssl session cache, modern ciphers, timeouts, gzip, a `limit_req` zone on login/register, static asset caching, request-timing access log); `container.yml` gained an `nginx -t` CI step. Verified live over real HTTPS in `deploy/standalone-tls` — headers present and correct, CSP nonce matches between response header and rendered HTML, rate limit genuinely throttles rapid `/login` attempts, map/chart page renders with no CSP violations.
  - **A Linux host's first `docker compose up` on `deploy/standalone-tls/` can fail with "unable to open database file"** if the bind-mounted `./data` isn't owned by the container's fixed non-root user (`useradd --uid 1000` in the Dockerfile — it never runs as root). Fix: `sudo chown -R 1000:1000 data` before starting. Same root cause as the earlier Windows/Git-Bash `/data` permission gotcha, different trigger (there it was path-mangling; on a fresh Linux host it's genuinely an ownership mismatch, since Docker Desktop's own default ownership when auto-creating the bind-mount target doesn't line up with the app's `uid 1000`).
- **Server production plan, batch 3 (S4) — MERGED (2026-09-04)**, PR #12. New `web_sessions` table (migration `c9e96b644fd5`): the session cookie now carries a `session_id` (not the `user_id` directly), resolved server-side on every request via `resolve_web_session()` (`app/auth/web_sessions.py`), which enforces a 30-day absolute lifetime and a 14-day idle timeout and rejects a revoked row. Logout now actually revokes the row (previously it only cleared the browser cookie, so a copied cookie survived logout for its full 30-day signature lifetime). Password change and admin disable/reset-password cascade to revoke a target's web sessions, same as they already did for device tokens. New "Sessions" panel on `/settings` (list + per-session revoke), mirroring `/devices`. Migration verified against the real `deploy/standalone-tls` production database with no errors; the pre/post-logout cookie-replay behavior confirmed end-to-end against the pulled GHCR image.
  - **`SR_ADMIN_PASSWORD` only takes effect on the very first bootstrap** (`bootstrap_admin_if_needed` no-ops once any user row exists — see `server/app/auth/bootstrap.py`). Editing it in a deployment's `.env` after that first run has **no effect** on the existing admin account; it's not a "reset password" knob. Caused a confusing false-alarm 401 while verifying #12 — the actual current password has to be obtained separately (e.g. from whoever last used `/settings` to change it), not assumed from `.env`.
- **Server production plan, batch 4 (R1 + R3) — MERGED (2026-09-04)**, PR #13. `LocalFileBlobStore.put()` now writes to a temp file + `fsync` + atomic `os.replace()`, so a crash mid-write can't leave a committed row pointing at a truncated blob. Every delete route (API + web, activities + admin bulk-delete) now calls `session.commit()` explicitly before deleting the blob synchronously, rather than deleting first or relying on a `BackgroundTask` — testing against the real container showed a `BackgroundTask` runs as part of *sending* the response, before `db_session`'s own post-yield commit, not after, so it can't guarantee this ordering (a bug in the first draft, caught before merge). New `simple-runner-server gc [--apply]` CLI: dry-run reports orphan blobs (no row) and rows with a missing blob; `--apply` deletes the orphans. Concurrent duplicate uploads of the same `client_activity_id` (the phone's timeout-then-retry path) no longer surface as a 500 — the insert is wrapped in `session.begin_nested()` and an `IntegrityError` is caught; the loser's blob is discarded and the winner's row returned as if it had been a duplicate from the start. **Caught in testing: after a caught `IntegrityError` inside `begin_nested()`, the `Session` needs an explicit `session.rollback()` before it's usable again** — without it the next statement raises `PendingRollbackError` instead of surfacing anything useful; this is documented SQLAlchemy behavior, not SQLite-specific (confirmed with a minimal reproduction against the default engine, no custom pragmas involved). 149 tests (10 new), `ruff`/`mypy --strict`/Snyk clean, `openapi.json` unchanged (no API surface change). Verified end-to-end against the real pulled GHCR image in `deploy/standalone-tls`: upload/delete via curl and blob presence confirmed with `docker exec find`, `gc` dry-run and `--apply` against a planted orphan, and user-confirmed manually via the web UI (upload/delete, admin bulk user-delete with activities, a normal re-upload).
- **Server production plan, batch 5 (R2) — MERGED (2026-09-04)**, PR #14. `_enable_sqlite_pragmas` now sets `PRAGMA busy_timeout=5000` (two connections racing a write now wait briefly instead of failing immediately with "database is locked") and `PRAGMA synchronous=NORMAL` (safe with WAL — a small durability trade for less per-write I/O). `get_current_user`'s bearer-token path now only bumps `DeviceToken.last_used_at` when it's null or older than 60s, mirroring the pattern `app/auth/web_sessions.py`'s `resolve_web_session()` already used for `WebSession.last_seen_at` (that S4 code already anticipated R2 and left a comment pointing at it). 154 tests (5 new, including a genuine 10-thread concurrent-write test), `ruff`/`mypy --strict`/Snyk clean, `openapi.json` unchanged. Verified against the real pulled-then-rebuilt container: `PRAGMA busy_timeout`/`journal_mode` confirmed via `docker exec`, and two consecutive bearer requests within a second produced byte-identical `last_used_at` timestamps in `/api/v1/me/devices`.
- **Server production plan, batch 6 (R5) — MERGED (2026-09-04)**, PR #15. New `app/audit.py`: a dedicated `app.audit` logger (`configure_logging()`, called from `cli.run()`) plus `log_audit_event()`, emitting structured `event=... actor_id=... target_id=... client_ip=...` lines — never a token, cookie, or password. Wired into every security-relevant route (both API and web layers, since R9's API/web dedup hasn't happened yet): `login.success`/`login.failure`, `token.revoked`, `session.revoked`, `user.created/promoted/demoted/enabled/disabled/password_reset/deleted`, `activity.deleted`. Admin password reset converted from `hx-prompt` (which risked landing in proxy/access logs via the `HX-Prompt` request header) to an inline expand-in-row form (`admin_user_row.html`/`admin_user_password_form.html`, new `GET .../password-form` and `.../password-form/cancel` routes). nginx's existing `log_format` already excludes request headers — confirmed, no change needed there.
  - **Caught a real production bug while wiring this up**: `alembic/env.py`'s `fileConfig(config.config_file_name)` call defaults to `disable_existing_loggers=True`, which silently disables every logger not explicitly listed in `alembic.ini` — including the brand-new `app.audit` logger, since `cli.run()` always calls `migrate()` (which triggers this) immediately after `configure_logging()`. Without `disable_existing_loggers=False` (now set explicitly), audit logging would have been dead in production from the very first startup despite every unit test passing, because tests exercise migrations differently than the real `cli.run()` sequence — this only surfaced once caplog-based tests were written and a record inexplicably had `logger.disabled == True`. Confirmed fixed by checking `docker logs` for real `event=login.success` lines after the fix, not just passing tests.
  - 166 tests (19 new: 7 audit-log + 7 web password-form + reformats), `ruff`/`mypy --strict`/Snyk clean, `openapi.json` unchanged (web routes stay `include_in_schema=False`). Verified end-to-end against the real rebuilt container: login success/failure, admin password-reset via the new inline form (against a disposable test user, not a real account), and user deletion all produced correct `docker logs` audit lines.
- **Server production plan, batch 7 (R6 + R7) — MERGED (2026-09-04)**, PR #16. `RequestValidationError` (422) and unhandled exceptions now return the same `{"error": {"code", "message"}}` contract as every other API error; a plain browser navigation gets a rendered HTML page (`not_found.html` / new `error.html`) instead of JSON for the same failures (confirmed live: `/register` disabled and `/admin/users` as a non-admin both now 404 with `text/html`, previously JSON). Unhandled exceptions get a correlation id (logged via a new `app.errors` logger, returned in `X-Correlation-Id` and the API body). `_VERSION` also guards `importlib.metadata.version()` returning `None`, not just `PackageNotFoundError`. Activity templates read `client_summary` fields via `.get(...)` instead of Jinja attribute access, so a summary missing an optional field (e.g. `avg_speed_mps`) renders `—` instead of 500ing (confirmed live by uploading an activity with the field omitted). All server-rendered timestamps (activities, devices, sessions, admin users) now render as `<time datetime="ISO...">` and are formatted client-side in the viewer's local timezone via a small nonce'd script in `base.html`, replacing hardcoded UTC `strftime`. Also brought both `.env.example` files up to date with every `SR_*` setting `app/config.py` reads (`SR_ALLOW_REGISTRATION`, `SR_MAX_GPX_BYTES`, `SR_LOG_LEVEL`, `SR_DATABASE_URL`, `SR_DATA_DIR` had never been documented) — unrelated to R6/R7 but folded into the same PR since it was pure missed documentation.
  - **FastAPI's `TestClient` re-raises unhandled exceptions by default instead of invoking a registered `Exception` handler** (`raise_server_exceptions=True`) — the shared `app_client` fixture uses that default, so the new tests exercising the 500 path construct their own `TestClient(app_client.app, raise_server_exceptions=False)` around the same app instance rather than adding a second app-creation path.
  - 174 tests (8 new in `tests/test_error_handling.py`), `ruff`/`mypy --strict`/Snyk clean, `openapi.json` unchanged (error-body shape is a runtime change only, not a schema change). Verified end-to-end against the real rebuilt container over HTTPS: 422 shape, both HTML 404 cases, and the missing-`avg_speed_mps` activity page all behaved as intended with no tracebacks in `docker logs`.
- **Server production plan, batch 8 (D2) — MERGED (2026-09-04)**, PR #17. `deploy/standalone-tls/docker-compose.yml` hardened: `app` runs `read_only: true` (only `/data`, already bind-mounted, and a new `/tmp` tmpfs are writable — it never writes anywhere else), `cap_drop: [ALL]`, `security_opt: [no-new-privileges:true]`, a 512m memory limit, and rotated JSON logging. `proxy` pinned `nginx:alpine` → `nginx:1.27-alpine` (was a floating tag), same security/logging treatment, `cap_drop: [ALL]` plus the four capabilities (`CHOWN`, `SETUID`, `SETGID`, `NET_BIND_SERVICE`) its root→worker-user privilege drop actually needs, and a real `healthcheck` against `https://127.0.0.1/healthz`. `depends_on` now waits for `app`'s `service_healthy` condition rather than just "started". No server code changed — this is deploy-only. `app`'s image tag deliberately stays `:latest` rather than pinning to a `sha-<commit>` tag (discussed with the user, who preferred keeping the simple `pull && up -d` update flow for this single-operator homelab deployment); the README documents pinning to a `sha-<commit>` tag as the rollback procedure instead, using the tags `container.yml` already pushes on every merge.
  - **`cap_drop: [ALL]` alone breaks nginx at startup** with `chown("/var/cache/nginx/client_temp", 101) failed (1: Operation not permitted)` — its master process starts as root specifically to bind ports 80/443 and then drops to the `nginx` worker user, which needs `CHOWN`/`SETUID`/`SETGID`/`NET_BIND_SERVICE` re-added even under an otherwise-fully-dropped capability set. Confirmed by reproducing the failure directly (`docker run --cap-drop=ALL nginx:1.27-alpine`) before adding the four back.
  - **BusyBox `wget` (the only HTTP client in `nginx:alpine`) follows redirects by default** — a healthcheck against plain `http://127.0.0.1/` silently chases the app's http→https redirect out to whatever `Location` header comes back (confirmed by pointing it at a throwaway 301 responder, which it followed all the way to an unrelated external host). Fixed by healthchecking `/healthz` directly over `https://` with `--no-check-certificate` instead of `/`.
  - Verified against the real container: `docker compose config` valid; both services report `(healthy)`; `docker exec app touch /x` → `Read-only file system` while `/tmp` and `/data` stayed writable (confirmed with a real login and a device-token delete round-tripping through the hardened stack); `nginx -t` passes against the pinned tag and the real `nginx.conf` (mirrors the `container.yml` CI step); `docker logs` clean throughout on both containers.
- **Server production plan, batch 9 (D3) — MERGED (2026-09-04)**, PR #18, plus a follow-up deploy-only commit restructuring the live dev stack. `Settings` (`app/config.py`) now falls back to reading any setting from `/run/secrets/SR_<FIELD>` (Docker Compose's native `secrets:` mount point) when the plain env var isn't set — an env var of the same name still wins if both are present. `secrets_dir` is only passed to pydantic-settings when `/run/secrets` actually exists, since the library warns unconditionally otherwise (confirmed it would fire on every `Settings()` construction, including every test, if always passed). `bootstrap_admin_if_needed` now logs a one-line warning (`app.bootstrap`, never the password) when `SR_ADMIN_PASSWORD` is still set once at least one user already exists — it only ever does anything on the very first startup.
  - `deploy/standalone-tls/docker-compose.yml` now actually uses this: a `secrets:` block mounts `./secrets/secret_key.txt` (gitignored, per-deployment) at `/run/secrets/SR_SECRET_KEY` for the `app` service, so `SR_SECRET_KEY` no longer needs to be a plain env var in `.env` at all. Confirmed live against the real dev stack: `docker inspect`'s `Config.Env` no longer lists `SR_SECRET_KEY`, the mounted file has the same value the key always had (not rotated, so existing sessions stayed valid), and login still worked. `SR_ADMIN_PASSWORD` stays a plain env var (it's not worth a secrets file once the bootstrap warning already flags it as unused) but is now commented out in the dev stack's `.env` with the value preserved in a comment for reference when testing — this silences the startup warning without losing the ability to look up the password.
  - Both `.env.example` files now steer new deployments towards the secrets-file approach for `SR_SECRET_KEY` (and note the same option for `SR_ADMIN_PASSWORD`), rather than just documenting the plain env var.
  - 179 tests (5 new: 2 in `test_config.py` for the secrets-dir fallback and env-wins-over-file precedence, 3 in new `test_bootstrap.py` for the warning), `ruff`/`mypy --strict`/Snyk clean (one new-looking High Snyk finding on `env_prefix="SR_"` traced to a pre-existing unmodified line that only shifted line number — false positive, not a real change), `openapi.json` unchanged. Verified end-to-end against both a throwaway secrets-only stack (no plain env var set at all, `docker inspect` confirmed clean) and the real dev stack after restructuring.
- **Project renamed simple-runner → simple-activity-tracker (2026-09-05).** Catches the repo/package/image identity up to the app's own display name (changed in an earlier rebrand, commit `ef6753c`, but never applied beyond doc/UI text). GitHub repo renamed (`gh repo rename` — old clone URL auto-redirects); `server/pyproject.toml`'s package name, CLI entrypoint, and `server/Dockerfile`'s `CMD` all became `simple-activity-tracker-server`; `.github/workflows/container.yml` now builds/pushes `ghcr.io/sjefferson99/simple-activity-tracker-server` (GHCR can't rename a package — old `simple-runner-server` left in place, unused, flipped nowhere, just dormant); `deploy/standalone-tls/docker-compose.yml` points at the new image; `SR_DATABASE_URL`'s default filename became `simple_activity_tracker.db`; device token prefix became `satdt_` for newly issued tokens (old `srdt_` tokens keep working — verification is SHA-256 hash lookup only, never prefix parsing); `docs/how-simple-runner-works.{html,pdf}` renamed to `docs/how-it-works.*`. Historical status-log entries elsewhere in this file and in `docs/PLAN.md`/`docs/WEB-PLAN.md` deliberately still say `simple-runner`/`simple-runner-server` — they're dated records of what was actually true at the time, not living reference text.
  - **Caught during verification, not planning: `server/Dockerfile`'s `CMD ["simple-runner-server", "run"]` was missed on the first pass** despite an earlier repo-wide grep — the container failed outright (`exec: "simple-runner-server": executable file not found in $PATH`) the first time the renamed image was actually started, not caught by review alone. Fixed and rebuilt before continuing; a reminder that a rename this size needs a real container start, not just grep, as the final check.
  - **Migrated the live dev-stack database, not just a fresh one.** Stopped the `app` container first (clean shutdown lets SQLite's WAL checkpoint fully into the main file — confirmed the `-shm`/`-wal` files were gone after stop, so a plain `mv` of just the `.db` was safe here), renamed `data/simple_runner.db` → `data/simple_activity_tracker.db`, verified row counts via a direct `sqlite3` query before restarting, then confirmed via a real login (same user id as before) and a direct query that the pre-existing 11 activities (owned by a different, non-admin test account) were intact.
  - New GHCR package defaulted to private on creation, same as the original did at W0 — flipped to public via the GitHub web UI (same manual step as before; `gh auth token`'s default scope still lacks `read:packages`/`write:packages`, unresolved from W0's note since it needs an interactive `gh auth refresh` the user has to run).
  - Verified end-to-end against the real CI-built, GHCR-pulled image (not just a local build): clean startup, `/healthz` 200, login with existing credentials, new tokens carrying the `satdt_` prefix.
- **Server production plan, batch 10 (D5) — MERGED (2026-09-05)**, PR #19. `.github/dependabot.yml` covers `uv` (server), `pub` (mobile), `github-actions`, and `docker` (server Dockerfile + both compose directories), all weekly. `server.yml` gained a `pip-audit` step (`uv export --no-dev --format requirements-txt --no-hashes`, piped through `grep -v '^-e '` before `pip-audit --strict`); `container.yml` gained an `aquasecurity/trivy-action` scan (HIGH/CRITICAL, `ignore-unfixed`) against the built image. Every `uses:` across all three workflows (`server.yml`, `container.yml`, `flutter.yml`) is now SHA-pinned with a version comment, pinned to each action's currently-used major's latest patch rather than bumping majors. No application code changed. Also fixed `docs/SERVER-PRODUCTION-PLAN.md`'s §7 checklist, which hadn't been updated to reflect R2/R5/R6/R7 already being merged in earlier batches.
  - **`pip-audit -r` can't parse the `-e .` editable-install line `uv export` emits for the package's own local install** — confirmed by reproducing the exact failure (`Invalid requirement: : Expected semicolon...`) before adding the `grep -v '^-e '` filter; the CI step reflects a verified fix, not a guess. Verified locally against the real lockfile: "No known vulnerabilities found".
  - Verified: full local check suite clean (179 tests, `ruff`/`mypy --strict` clean — no Snyk code scan needed since no first-party app code changed, only CI/config YAML). CI itself green on the PR (Server, Container, Flutter all pass), confirming the new pip-audit and Trivy steps run successfully, not just parse.
  - Also did a repo branch cleanup in the same session: deleted 10 stale merged remote branches and 12 stale local branches (including 3 dangling worktree refs left over from the `simple-runner` → `simple-activity-tracker` folder rename), and enabled GitHub's "automatically delete head branches" repo setting so future PR branches clean up on merge with no manual step — confirmed working on this very PR (origin branch auto-deleted within seconds of merge).
- **Server dev/test workflow now documented** — see "Server dev/test workflow" section below. Follow it for every server change: branch per plan item/group, local integration test in a real container (tag the local build as the exact `ghcr.io/...:latest` reference so `deploy/standalone-tls/docker-compose.yml` picks it up with no edits), user sign-off in the running dev-stack container, then commit/push/PR, then after merge pull the real rebuilt image and confirm. `deploy/standalone-tls/.env` on this dev machine is not sensitive — read it directly for admin credentials when testing, rather than asking the user each time.
- Update this section as work progresses (phase started/done, deviations from docs/PLAN.md).

### Toolchain notes learned during Phase 0 (beyond docs/PLAN.md §1)

- Flutter has no `winget` package — installed via `git clone -b stable` to `C:\git\flutter` (added to user PATH), not `C:\flutter`.
- `sdkmanager` requires `JAVA_HOME` pointed at the Android Studio JBR (`C:\Program Files\Android\Android Studio\jbr`) or it fails with "Java version 17 or higher is required" (it was picking up the stale Java 8 on PATH).
- The Gradle-driven NDK auto-download can silently produce a **corrupt/incomplete install** (missing `source.properties` and several top-level dirs) if interrupted — if a build fails with "did not install NDK ... into ...\Sdk", delete `%LOCALAPPDATA%\Android\Sdk\ndk\<version>\` entirely and let it redownload rather than trying to repair it.
- Orphaned `flutter run` processes (e.g. left attached to a device after switching targets) are worth cleaning up, but they are **not** the usual cause of `build\` lock errors — see the Gradle daemon note under Phase 1 for the real fix.
- A physical phone can drop to adb "offline" over USB; `adb kill-server && adb start-server` alone doesn't always fix it — a cable reseat usually does.

### Toolchain notes learned during Phase 1

- **`geolocator`'s `ForegroundNotificationConfig.enableWakeLock: true` requires the `android.permission.WAKE_LOCK` manifest permission.** Without it, the whole geolocator event channel throws a `SecurityException` and silently delivers zero location samples (no crash, no error surfaced to the app — just "stuck acquiring GPS forever"). We don't need it: `wakelock_plus` already keeps the screen on during tracking, so `enableWakeLock` is left at its default `false` rather than adding the permission for a redundant lock. If GPS acquisition mysteriously stops working after touching `AndroidSettings`/`ForegroundNotificationConfig`, check `adb logcat` for `EventChannel...geolocator_updates_android` — that's where this exception surfaces, not anywhere Dart-side.
- **Recurring `AccessDeniedException` / "Unable to delete directory" under `build\app\intermediates\...`** (seen in `extractReleaseNativeSymbolTables`, `mergeDebugNativeLibs`, `mergeDebugAssets`, `merged_native_libs\...\arm64-v8a`). It affects **both debug and release** builds — an earlier note here blamed release mode and AV scanning; that was wrong. **The holder is the Gradle daemon itself**: the failing paths are empty directories, and once `./gradlew --stop` runs, `Remove-Item -Recurse -Force` on `build\` succeeds immediately and the next build is clean. Clearing individual directories without stopping the daemon just moves the failure to the next directory. **Fix: `cd mobile\android && ./gradlew --stop`, delete `mobile\build\`, rebuild.** (`flutter clean` is not enough on its own — it hits the same locks.) This recipe has since been applied successfully more than once, so treat it as the first thing to try, not a guess.
- **`flutter install` does not rebuild.** It deploys whatever APK is already sitting in `build\app\outputs\`, so after a code change it will happily install the *previous* binary and the change appears not to have worked. Run `flutter build apk --debug` first, or just use `flutter run`. (This bit us on the debug-banner change — the fix looked broken until a screenshot showed the old APK had been installed.)
- **Verify device-visible changes with a screenshot** (`adb exec-out screencap -p > file.png`, then read the image) rather than assuming a successful install means the change is live.

## Quick facts (details in docs/PLAN.md §1)

- Windows 11, PowerShell. Android SDK at `%LOCALAPPDATA%\Android\Sdk`; Android Studio at `C:\Program Files\Android\Android Studio` (use its `jbr` as the Gradle JDK — the Java 8 on PATH is too old).
- Set up during Phase 0 and still in place: cmdline-tools installed, SDK licences accepted, Windows Developer Mode enabled (needed for Flutter plugin symlinks), and one AVD named `Pixel8_API36`.
- Test on emulator (simulated GPX routes) for iteration, physical Android phone over USB for real GPS truth.

## Commands

The Flutter app lives in `mobile/` (moved there when the web app work started — see
docs/WEB-PLAN.md). Run these from `mobile/`, or `cd mobile` first:

```powershell
flutter doctor -v        # toolchain health (only Android section must be green)
flutter pub get
flutter analyze          # must be clean before finishing any task
flutter test             # must pass before finishing any task
flutter run              # runs on the connected device/emulator
```

## Server dev/test workflow (use every time we work on `server/`)

Applies to work driven by docs/SERVER-PRODUCTION-PLAN.md or any other server change of
more than trivial size. One plan item (or tightly related group, e.g. "S5 + D1 together")
= one branch = one PR, following the plan's delivery order.

1. **Branch per item/group.** `git checkout main && git checkout -b server-<short-name>`
   (matches existing naming: `server-s1-secret-key-required`, `server-s2-input-validation`).
   Never commit server work straight to `main`.
2. **Implement with tests.** Every behavior change gets a test. Run from `server/`:
   `ruff check . && ruff format --check . && mypy app && pytest` (use the full path to
   `uv.exe` under `%LOCALAPPDATA%\Microsoft\WinGet\Packages\astral-sh.uv_...` from Bash —
   see the Toolchain notes above). Regenerate `openapi.json`
   (`uv run python -m app.openapi_export > openapi.json`) if the API surface or request
   schema changed, and diff it — new/changed constraints are expected drift, anything else
   isn't. Run `snyk_code_scan` on `server/` and fix any new finding (ignore the ~29-32
   known low-severity "hardcoded password" hits in `tests/`, all pre-existing literals).
3. **Local integration test before asking for a commit.** For each batch of items being
   verified together, build a throwaway local branch that merges every relevant PR branch
   (or just the current feature branch if it's alone), then verify it in a *real* container
   — unit tests alone don't exercise startup behavior, container networking, or the
   deployed nginx/TLS layer:
   - `git checkout main && git checkout -b integration-<short-name>-local-test && git merge --no-edit <branch-1> <branch-2> ...`
   - Run the full check suite (`ruff`/`mypy`/`pytest`/openapi diff/Snyk) on the merged tree.
   - Build the image from that branch and tag it **exactly** `ghcr.io/sjefferson99/simple-activity-tracker-server:latest`
     (`docker build -t ghcr.io/sjefferson99/simple-activity-tracker-server:latest server/`) so
     `deploy/standalone-tls/docker-compose.yml` picks it up with no file edits — this is
     the dev host's real stack (persistent, own `.env`, real self-signed cert, real
     `./data`), not a disposable one. Only tag `:latest` locally like this for a
     verification pass immediately before merging; don't leave a stale local build
     shadowing the real tag longer than that.
   - `cd deploy/standalone-tls && docker compose up -d`, confirm `docker logs
     standalone-tls-app-1` shows a clean migrate+bootstrap+startup with no tracebacks, the
     app container reports `(healthy)`, and `curl -sk https://127.0.0.1/healthz` returns
     `{"status":"ok",...}`.
   - Exercise the actual behavior that changed via `curl` (auth via `/api/v1/auth/login`,
     then bearer requests; web login needs `-c cookies.txt` + `X-Requested-With: htmx` on
     POSTs) — reproduce each plan item's "verified during review" probe against the new
     code and confirm the fix. From Git Bash: prefer `-F field=<file` (multipart) over
     `--data-urlencode field=@file` for large payloads — the latter sends the literal `@path`
     string here, not the file's contents.
   - Delete the local `integration-*` test branch once done — it only ever exists locally,
     never pushed.
4. **Hand off to the user.** Report what was verified (each probe/behavior, pass/fail,
   test counts, Snyk result) and ask the user to log into the same running dev-host stack
   and confirm before anything is pushed. Do not push or open a PR before this sign-off.
5. **On confirmation: commit, push, open the PR, stop.** One commit per item/group
   (ask first, per the git commit rule below), `git push -u origin <branch>`, then
   `gh pr create` with a summary + test plan (both the automated checks and what was
   manually verified in the container). Wait for the user to merge — do not merge
   server PRs automatically.
6. **After the user merges:** `gh pr checks <n>` to confirm CI green, watch/confirm the
   `Container` workflow pushes to GHCR, `docker pull ghcr.io/sjefferson99/simple-activity-tracker-server:latest`,
   restart the dev stack on the real pulled image (`docker compose up -d` in
   `deploy/standalone-tls/`), confirm healthy, and reconcile local `main` with
   `origin/main` (`git checkout main && git pull`) before starting the next batch.

## Architecture rules (from docs/PLAN.md §3 — follow strictly)

- Feature-first layout under `mobile/lib/`: `app`, `core/{location,units,files}`, `domain/{models,tracking}`, `features/live_run`.
- `core/units` and `domain/` are **pure Dart** — no `flutter` imports; unit-test everything there.
- The app touches GPS only through the `LocationService` interface (`core/location`) and our own `LocationSample` model — geolocator types never leak out of `core/location/`.
- Metric tiles are driven by a `MetricSpec` list even while the layout is static — this is what makes the future customizable display cheap.
- State: Riverpod, one-way flow (service stream → Notifier → immutable state → widgets).

## Conventions

- Units displayed: km/h and min/km. All conversions/formatting via `core/units`, nowhere else.
- Speeds are stored internally in m/s (as GPS provides); convert only at display time.
- **Always ask before running `git commit`** — even at a meaningful milestone, even if a commit earlier in the same session was already approved. Summarize what would be committed and wait for a go-ahead.
- Stay on `main` unless told otherwise.
